#include "argument_validator.h"

namespace fsp::cli
{
    // ArgumentValidator::validate: placeholder for validating CLI arguments
    // after parsing. No validation rules are currently enforced.
    void ArgumentValidator::validate()
    {
    }
}
