#include "config_builder.h"

namespace fsp::cli
{
    // ConfigBuilder::build: build runtime configuration from parsed
    // arguments. Currently a stub; real configuration assembly can be
    // implemented when additional CLI options are needed.
    void ConfigBuilder::build()
    {
    }
}
